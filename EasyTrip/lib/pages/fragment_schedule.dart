import 'package:flutter/material.dart';

class ScheduleFragment extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('Schedule Fragment', style: TextStyle(fontSize: 24)),
    );
  }
}
