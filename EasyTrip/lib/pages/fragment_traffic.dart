import 'package:flutter/material.dart';

class TrafficFragment extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('Traffic Fragment', style: TextStyle(fontSize: 24)),
    );
  }
}
