import 'package:flutter/material.dart';

class SearchActivity extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Search Activity'),
      ),
      body: Center(
        child: Text('This is the search activity.'),
      ),
    );
  }
}
